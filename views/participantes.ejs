<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <title>Historial Mis Eventos</title>
    <link rel="icon" sizes="192x192" href="/img/icon.png">
</head>
<body>
    <div class="container border-bottom" id="general-horizontal">
        <div class="border-bottom mb-3">
            <%- include('../partials/headerUsuario.ejs') %>
        </div>
        
        <% for (let i = 0; i < inscripciones.length; i++) { %>
            <% if (i % 2 === 0) { %>
                <!-- Abrir una nueva fila cada dos usuarios -->
                <div class="row mb-3">
            <% } %>
    
            <!-- Usuario -->
            <div class="col-6">
                <div class="border border-black border-3 rounded-5 p-3">
                    <div class="d-flex align-items-center">
                        <div class="me-3">
                            <img src="/img/usuario.png" class="img-fluid rounded-circle" style="max-width: 80px;">
                        </div>
                        <div class="">
                            <h4><b>Nombre: </b><%= inscripciones[i].nombre %></h4>
                            <h5><b>Facultad: </b><%= inscripciones[i].facultad %></h5>
                            <div class="mt-3"></div>
                        </div>
                    </div>
                </div>
            </div>
    
            <% if (i % 2 === 1 || i === inscripciones.length - 1) { %>
                <!-- Cerrar la fila después del segundo usuario o al final -->
                </div>
            <% } %>
        <% } %>
    </div>
   
   <!--------------------------------MODALES--------------------------->

   <!-- Incluir el modal de accesibilidad desde su EJS -->
   <%- include('../partials/accesibilidadModal.ejs') %>

    <!--------------------------------LIBRERIAS--------------------------->

    <!-- Librería con popper incluida, necesaria para los modales -->
    <script src="/js/bootstrap.bundle.min.js"></script>

    <!-- Script de accesibilidad -->
    <script src="/js/accesibilidadScript.js"></script>
</body>
