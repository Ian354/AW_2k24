<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <title>Cola Evento</title>
    <link rel="icon" sizes="192x192" href="/img/icon.png">
</head>
<body>
    <div class="container">
        <div class="border-bottom mb-3">
            <%- include('../partials/headerUsuario.ejs') %>
        </div>
        
        <!--Cuerpo del HTML-->
                    
        <div class="container rounded-5 p-3">
            <div class="text-center">
                <h3><u>Usuarios Apuntados</u></h3>
            </div>
            <% for (let i = 0; i < inscripciones.length; i++) { %>
                <% if (i % 2 === 0) { %>
                    <!-- Abrir una nueva fila cada dos usuarios -->
                    <div class="row mb-3">
                <% } %>
        
                <!-- Usuario -->
                <div class="col-6">
                    <div class="border border-black border-3 rounded-5 p-3">
                        <div class="d-flex align-items-center">
                            <div class="me-3">
                                <img src="/img/usuario.png" class="img-fluid rounded-circle" style="max-width: 80px;">
                            </div>
                            <div>
                                <h5><b>Nombre: </b><%= inscripciones[i].nombre %></h5>
                                <h6><b>Facultad: </b><%= inscripciones[i].facultad %></h6>
                                <div class="mt-3">
                                    <form action="/usuario/eventos/eliminar/<%= evento %>/<%= inscripciones[i].ID %>" method="post" class="d-inline">
                                        <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
        
                <% if (i % 2 === 1 || i === inscripciones.length - 1) { %>
                    <!-- Cerrar la fila después del segundo usuario o al final -->
                    </div>
                <% } %>
            <% } %>
        </div>
        
        <div class="container rounded-5 p-3">
            <div class="text-center">
                <h3><u>Lista de Espera</u></h3>
            </div>
            <% for (let i = 0; i < listaEspera.length; i++) { %>
                <% if (i % 2 === 0) { %>
                    <!-- Abrir una nueva fila cada dos usuarios -->
                    <div class="row mb-3">
                <% } %>
        
                <!-- Usuario -->
                <div class="col-6">
                    <div class="border border-black border-3 rounded-5 p-3">
                        <div class="d-flex align-items-center">
                            <div class="me-3">
                                <img src="/img/usuario.png" class="img-fluid rounded-circle" style="max-width: 80px;">
                            </div>
                            <div>
                                <h5><b>Nombre: </b><%= listaEspera[i].nombre %></h5>
                                <h6><b>Facultad: </b><%= listaEspera[i].facultad %></h6>
                                <div class="mt-3">
                                    <form action="/usuario/eventos/eliminar/<%= evento %>/<%= listaEspera[i].ID %>" method="post" class="d-inline">
                                        <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
        
                <% if (i % 2 === 1 || i === inscripciones.length - 1) { %>
                    <!-- Cerrar la fila después del segundo usuario o al final -->
                    </div>
                <% } %>
            <% } %>
        </div>
    </div>
   
    
    <!--------------------------------LIBRERIAS--------------------------->

    <!--Librería con popper incluida, necesaria para los modales-->
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>

